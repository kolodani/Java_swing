import javax.swing.ImageIcon;
import javax.swing.JFrame;
import java.awt.Color;

public class Main {

    public static void main(String[] args) {
        /*
         * 
         * JFrame frame = new JFrame(); // Create a Frame
         * frame.setTitle("JFrame title goes here"); // Set the title of the frame
         * frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // Exit the application
         * when the frame is closed
         * frame.setResizable(false); // Prevent the frame from being resized
         * frame.setSize(420, 420); // Set the size of the frame in pixels, x, y
         * frame.setVisible(true); // Make the frame visible
         * 
         * ImageIcon image = new ImageIcon("src/Icon.png"); // Create an ImageIcon
         * frame.setIconImage(image.getImage()); // Change icon of the fram
         * frame.getContentPane().setBackground(new Color(123,50,250)); // Change the
         * background color of the frame
         */

        new MyFrame();
    }
}
